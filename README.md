# CAMP dinner bot

Telegram bot for asking what a child would like for dinner and replying with a simple recipe plus a cute food image.

## Run locally

Set the bot token as an environment variable:

```bash
export TELEGRAM_BOT_TOKEN="your-telegram-bot-token"
python3 bot.py
```

The token must not be committed to Git.
